// The hasOwnProperty() method is used to check if an object has a specific property. It returns true if the object has the specified property, otherwise false.

const person = {
    name: 'John'
  };
  
  console.log(person.hasOwnProperty('name')); // Output: true
  console.log(person.hasOwnProperty('age')); // Output: false
  