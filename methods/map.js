const numbers = [1,2,3,4,5];
const square = function(numbers){
    return numbers*numbers;
} //function has to return value while using map function

const squarenummber = numbers.map(square);  //map function gives a new array.

console.log(squarenumber);