//short syntax for methods
const user1={
    name:"shraddha",
    age:90,
    about(){
        console.log(`${this.name},${this.age}`);
    }
}
user1.about();

//above is short syntax to right functions